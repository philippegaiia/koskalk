# 2026-08-29

## Code review: bd103fb3 (branch codex/ingredient-guidance-refresh)

Reviewed commit `bd103fb3` "fix: revalidate stale locales in all guidance modes" in worktree
`.worktrees/ingredient-guidance-refresh`. Review only, no code changes.

Change: `IngredientGuidanceChangePlanner::plan()` dropped the
`$mode === GuidanceLocalization` gate, so a text-identical locale with a stale
`source_fingerprint` now gets a `revalidate` decision in both guidance modes.

Observed (throwaway test, since deleted): the `revalidate` path sets `refreshMetadata = true`, and
`IngredientTranslationService::sync()` rewrites `origin` and `prompt_version` alongside the
fingerprint, so a stored `reviewer_edited` row becomes `ai_generated` on revalidation.

**CORRECTION — the above is NOT a defect.** I had not read the approved design spec
(`docs/superpowers/specs/2026-08-28-ingredient-guidance-refresh-corrections-design.md`). It mandates
this: line 52 says an approved `revalidate` updates "source fingerprint, origin, and prompt version";
line 101 says "AI-generated **or revalidated** rows receive AI provenance and the localization prompt
version". The behaviour is by design. I wrongly inferred intent from a test instead of the spec, and
over-read invariant "Reviewer edits cannot silently inherit AI provenance" (line 146) as implying the
converse. It does not. Retaining historical human authorship would need a design change — a separate
audit field — not a planner patch.

Also noted: `$mode` is now unused in `plan()` (and `$editedFields` already was); both call sites
still pass them — confirmed real, low-risk cleanup, not a blocker. The review-item increase is
intentional (design line 147 gates revalidation-only changes behind human review).

**Surviving item (new):** design spec line 47 still scopes revalidation to "a localization-only run",
but bd103fb3 widened it to both guidance modes. Commit updated the plan doc, not the design doc —
minor spec drift worth aligning.

Verification: 17 tests pass in the two changed files; 39 pass across the wider enrichment/
guidance suite; Pint clean. No PHPStan/Larastan in this project.

## Plan written: Production Bench navigation hierarchy (plan only, no code)

Owner asked for a plan (not implementation) to fix the flat Production Bench nav. Chose the
"group the model" option: declare a real 3-tier tree, keep every existing `active`/`subnavigation`
prop so the 27 Livewire views need zero edits.

Plan: `docs/superpowers/plans/2026-08-29-production-bench-navigation-hierarchy.md`

Structure discovered (this is the part worth remembering):
- Two nav tiers today: `components/production-bench/navigation.blade.php` (8 flat underline tabs)
  plus three separate section components (inventory / purchasing / production-settings) that render
  as detached chip rows, selected by an `if/elseif` chain in `page.blade.php`.
- `Tasks`, `Flash planner`, `Calendar` are `production-bench.production.*` routes but are rendered as
  peers of Inventory and Purchasing; `Production setup` sits detached at the far right. That is the
  "flatness".
- Proposed tree: Home | Inventory(3) | Production(5, one of which is Setup with 7 grandchildren) |
  Purchasing(5). Level-1 row drops 8 items → 4, which is what makes it fit `--container-app: 74rem`.
- Key trick: node **keys** are chosen to equal the `active`/`subnavigation` values views already
  pass, so resolution is a lookup and no view changes.

Test constraints found (would bite any redesign):
- `tests/Feature/ProductionBenchLayoutTest.php` asserts exact `aria-current="page"` **counts** and
  the literal class strings `border-[var(--color-accent)] text-[var(--color-ink-strong)]` and
  `bg-[var(--color-accent-soft)] text-[var(--color-accent-strong)]`; it also forbids
  `overflow-x-auto` and `-mb-px` in `navigation.blade.php` and greps all 27 views for props.
- `tests/Feature/ProductionBenchProductionSettingsTest.php:196` asserts
  `Lang::get('production_bench.navigation.production', [], 'en') === 'Production setup'` — that key
  and value must survive. Several other labels are pinned by `assertSee`.
- `production_bench.*` keys live only in `lang/en/production_bench.php`; they are **not** in
  `database/seeders/data/interface-translations.json`, so no translation-seed sync is needed. Other
  locales have no `production_bench.php` and fall back to English.

House conventions to reuse:
- Nav CSS belongs in `resources/css/app.css` `@layer components` as `sk-*` classes with `.is-active`
  (precedent: `.sk-workbench-tabs` / `.sk-workbench-tab.is-active`), not inline Tailwind strings.
- Production Bench is the app's **first** multi-level nav — no precedent to mirror, so name classes
  generically (`.sk-nav-*`) for reuse by Settings/Account later.

### Revision 2 (owner feedback) — decided, documented, do not re-ask

Owner feedback on the plan, applied:
1. "Home" → **"Dashboard"** (owner's final call — I had recommended "Production Bench Home" and was
   overruled; do not re-raise). I flagged that the global sidebar's top item is labelled "Overview"
   (not "Dashboard") and that ingredient/packaging pages have "Back to dashboard" links, so "Dashboard"
   could read as the app-wide one. Owner accepted the risk. Mitigations written into the plan: the
   destination's `page_heading` and `<h1>` already read "Production Bench" (`home.blade.php` line 4,
   `home-index.blade.php` line 4); the tab gets a scoped `aria-label` via a new
   `navigation.home_aria` = "Production Bench dashboard" key; and the tab sits isolated behind a
   divider at the head of the row. If it still confuses in use, `navigation.home` = "Bench dashboard"
   is a one-string fallback.
2. **Production setup promoted from a Production child to a top-level tab, relabelled "Settings".**
   Reasons: users cannot see settings exist; a Production-named tab cannot host inventory/purchasing
   settings. This collapsed the nav from 3 levels to 2 — level 3 CSS is now dormant.
   - Node **key** stays `production-setup` (invisible to users; renaming it means editing 6 Livewire
     views plus the layout test's view scanner). Only the *label* changes.
   - Settings sits **right-aligned** (`ml-auto`) behind a divider — a utility, not a workflow step.
   - Its 7 children are grouped inside the rail under eyebrow sub-headings: **Production**
     (Numbering, Batch sizes, Task types, Task sets, Working calendar) and **Resources**
     (Departments, Employees). Adding an Inventory or Purchasing group is then a one-line config
     change. Sub-headings are *not* a third indent level.
   - Relabelling requires updating `ProductionBenchProductionSettingsTest.php:196`, which asserts
     `navigation.production === 'Production setup'`.

**Latent key collision found (worth remembering):** `calendar` is needed by two nodes — Production's
"Production calendar" and Settings' "Working calendar". `SettingsIndex::mount()` derives `$section`
from `request()->routeIs()` and `settings-index.blade.php` passes it straight through as
`subnavigation`, so the settings child key really is the bare string `calendar`.

> **CORRECTED 2026-08-29 (implementation).** I first claimed renaming `mount()` to emit
> `'working-calendar'` was safe because "`$section` is only ever compared against `'all'` in the
> view". **That was wrong.** `settings-index.blade.php:145` also does
> `in_array($section, ['all', 'calendar'], true)` to decide whether to render the working-calendar
> block, so the rename silently hid the section and broke
> `ProductionBenchProductionSettingsTest::organizes production setup into focused submenu routes`.
> Lesson: **`$section` in `SettingsIndex` is overloaded — it drives both the subnavigation and the
> content filter. It must keep matching the tree's node keys verbatim.** The fix was reversed: keep
> `mount()` on `'calendar'` and key the tree node `calendar`. No collision actually exists either —
> see below.

## Project tooling notes

- `laravel/boost` ^2.5 installed (dev); guidelines synced to `AGENTS.md` / `CLAUDE.md`.
  `laravel/mcp` present only as a Boost dependency — not a direct require, no Truss MCP server.
- **No MCP connector is wired into the assistant session** (no `~/.workbuddy-ai/mcp.json`), so
  Boost tools are not callable; only its synced guidelines are readable as static context.
- Only automated quality gate is Pint (`vendor/bin/pint`). No static analysis configured.
- Test helpers (`guidanceResult`, `guidanceApplyText`, …) are defined at the bottom of
  `tests/Feature/IngredientGuidanceBatchReviewTest.php` and are file-scoped, not global.

## Production Bench navigation hierarchy — implemented (branch `codex/production-bench-navigation-hierarchy`)

Plan: `docs/superpowers/plans/2026-08-29-production-bench-navigation-hierarchy.md`. All five phases
done. Full suite: **2606 passed, 14 failed, 25 skipped** — all 14 pre-existing (see below).
Pint passed, `npm run build` passed, all 8 `.sk-nav-*` classes emitted (24 rules).

### What shipped

- `app/Support/ProductionBenchNavigation.php` (new) — declarative tree. Node shape
  `key, route, label, aria, icon, group, end, divider, children`. `end` (trailing-edge pin) and
  `divider` are **layout flags in data**, so the partial never hard-codes "the first/last item".
  Level 1: Dashboard | Inventory | Production | Purchasing | Settings(right-pinned).
- `navigation-items.blade.php` (new) — the single recursive partial; `data-level` is the only axis
  of visual differentiation. `navigation-icon.blade.php` (new) — 24 inline stroke SVGs.
- `navigation.blade.php` — now a thin resolver wrapper. `page.blade.php` — `if/elseif` chain deleted,
  all boolean props and `space-y-8` kept.
- `resources/css/app.css` — `.sk-nav-*` block in `@layer components`. Level 1 = 44px underline tabs;
  level 2 = 36px recessed drawer (`.sk-nav-rail`, `margin-left` + `border-left` 2px). Touch targets
  meet WCAG 2.5.8. Dormant `[data-level='3']` rule left in.
- Deleted `inventory-navigation`, `purchasing-navigation`, `production-settings-navigation`.
- `aria-current` semantics changed: **exactly one `page`** (the exact destination); ancestors get
  `aria-current="true"` + `.is-branch`.

### Three corrections to the plan (all recorded in the plan doc's header table)

| # | Plan said | Reality |
| --- | --- | --- |
| C1 | `production_bench.*` not in the translation catalogue → no sync needed | **903 rows are there**, and `InterfaceTranslationCatalogueTest` asserts exact key parity vs `lang/en/production_bench.php` across 6 locales. Every `lang/en` change must be mirrored. |
| C2 | Rename `SettingsIndex::mount()` `'calendar'` → `'working-calendar'` | **Unsafe** — `$section` is overloaded (see correction above). Reverted; the tree node is keyed `calendar` instead. |
| C3 | `calendar` collides between Production and Settings | **No collision.** `resolve()` locates level 1 first, then looks up `subnavigation` within that node's children only. `locate()` walks in declaration order and `production` precedes `production-setup`, so `active="calendar"` deterministically hits Production's calendar. |

### Traps worth remembering

- **`database/seeders/data/interface-translations.json` is bytewise-sorted with 2-space indent.**
  Rewriting with `indent=4` inflates it 1.21 MB → 1.42 MB. Always round-trip-verify first.
  Net change this time: **+4 / −1 rows, all `production_bench`** (`navigation.home_aria`,
  `production_runs`, `settings`, `settings_group_resources` added; `navigation.production` removed).
- **Reuse existing translation keys instead of inventing ones** — the Production calendar child reuses
  `production_bench.calendar.title`; the Settings "Production" sub-heading reuses
  `navigation.production_workflow`, so it can never drift from the level-1 tab label.
- `purchasing-navigation`'s hard-coded `aria-label="Purchasing sections"` is gone; the nav is now one
  landmark labelled "Production Bench". `ProductionBenchGoodsReceiptPagesTest:81` asserted that string
  and had to be retargeted.
- Symlinking `vendor` into a `git worktree` **breaks app bootstrap** — the autoloader resolves to
  the real path and you get `Call to undefined method Container::resourcePath()`. For a baseline
  comparison, diff the files directly instead of running tests in a scratch worktree.

### Pre-existing failures (14, none caused by this work)

- **6 of them are one bug.** Catalogue holds 7 stale rows at `ingredients` /
  `validation.translation_write_intent_*`, but `lang/en/ingredients.php` nests them under
  `editor.validation.*`. Present identically at HEAD. **Fix = delete those 7 rows** (the correct
  `editor.validation.*` rows already exist). Unblocks all 6.
- 3 × `StartIngredientIntakeResearchTest`: enum-vs-string comparison.
- 1 each: `IngredientEnrichmentTrustDimensionsTest`, `MediaStorageTest`,
  `RecipeWorkbenchDesignPolishTest`, `SearchComboboxAdoptionTest`, `WorkflowActionConsistencyTest`.

### Follow-up for the user

**The local DB is behind the catalogue** (891 `production_bench` rows vs 906). Needs
`php artisan translations:sync` then `translations:catalogue:import --mode=authoritative` before the
new Settings / Dashboard labels appear in non-English locales. Not run — it mutates the DB.

## Translation sync + catalogue import — done (owner approved)

### The blocker, and why my earlier recommendation was wrong

I had written "the DB is behind; run `translations:sync` then `translations:catalogue:import
--mode=authoritative`". Two things were wrong about that:

1. **The import was impossible, not just unwise.** `InterfaceTranslationCatalogue::read()` validates
   the *whole* catalogue and throws **before any DB write**. With the 7 stale
   `ingredients` / `validation.translation_write_intent_*` rows present, the command cannot run at all.
2. **`sync` and `import` are not the same class of operation.** `translations:sync` reads `lang/en`
   only (it never touches the catalogue), so it *was* runnable — but it only creates empty `text` rows,
   so it would not have landed the relabel. Worse, running sync-then-import would have inserted 58
   empty rows and then failed, leaving blank translations.

**Lesson: measure the blast radius before recommending a mutating command, and check whether the
command can even run — don't reason about it from the description.**

### What was run (with numbers measured first)

Dry comparison (catalogue vs. DB, no writes): 58 rows to create, **18** to overwrite
(17 `production_bench`, 1 `packaging`), 1 DB-only row that neither command deletes.

- `php artisan translations:sync` → 51 created, 2681 already present, 0 pruned
- `php artisan translations:catalogue:import --mode=authoritative` → 0 created, **69 updated**,
  2663 unchanged, 0 production values preserved

69 = the 51 empty rows `sync` had just created + the 18 predicted overwrites. Consistent.

### Catalogue change vs. HEAD is now +4 / −8

Added: `production_bench.navigation.{home_aria, production_runs, settings, settings_group_resources}`.
Removed: `production_bench.navigation.production` (renamed) and the 7 stale `ingredients` rows.
The correct `ingredients` / `editor.validation.translation_write_intent_*` rows already existed, so
deleting the stale ones was a pure deletion with no data loss.

### Result

Verified all locales resolve: `de` now reads `Dashboard` (was `Start`), `Einstellungen`,
`Produktionen`, `Ressourcen`; `fr`, `nl`, `es`, `it`, `pt_BR` all resolve too.

**Full suite went 14 failed → 8 failed** (2612 passed). All 6 catalogue failures cleared. The
remaining 8 are the unrelated pre-existing ones (3 × StartIngredientIntakeResearchTest enum-vs-string,
plus IngredientEnrichmentTrustDimensions, MediaStorage, RecipeWorkbenchDesignPolish,
SearchComboboxAdoption, WorkflowActionConsistency).

### Environment note

`APP_ENV=local`, DB is **`koskalk_restore_20260722_023001`** on 127.0.0.1 — a *restored backup*, not
the live dev DB. Worth knowing before anyone runs authoritative imports here.

---

## External review of the navigation work — evaluated (2026-08-29)

A second reviewer graded the committed implementation. I verified every finding against the code
rather than accepting or rejecting them. Two confirmed defects, both mine.

### Confirmed defect 1 — Settings is not right-aligned (P1)

`margin-inline-start: auto` on `[data-nav-end]` is inert. Level 1 renders inside a single
`.sk-nav-cluster` (verified: the settings page emits 3 clusters total = 1 at level 1 + the 2
Settings sub-groups at level 2). Compiled CSS:

```
.sk-nav-row{flex-wrap:wrap;align-items:center;display:flex}
.sk-nav-cluster{flex-wrap:wrap;align-items:center;gap:.25rem;display:flex}   <- no flex-grow
.sk-nav-item[data-level="1"][data-nav-end]{margin-inline-start:auto;...}
```

The cluster is a content-sized flex item, so the free space lives in `.sk-nav-row`, not in the
cluster. `margin-inline-start: auto` absorbs zero. Fix must be scoped to level 1 —
`.sk-nav-row[data-level='1'] > .sk-nav-cluster { flex: 1 1 auto; }` — otherwise the two Settings
sub-groups at level 2 get pushed apart too. Pest cannot compute layout; regression coverage has to
be a source-level assertion or a visual check.

### Confirmed defect 2 — the settings landing page announces the wrong current page (P1)

Probe on `GET /dashboard/production-bench/production/settings` printed:

```
branch  .../production-bench/production/settings              <- aria-current="true"
PAGE    .../production-bench/production/settings/numbering    <- aria-current="page"  WRONG
```

The browser is on `/settings`, which renders **all seven** sections, yet the Numbering link to a
different URL is announced as the current page.

Cause: `ProductionBenchNavigation::resolve()` line 165-167 ends in `?? $node['children'][0]`.

**The view already said what it wanted.** `settings-index.blade.php:1` passes
`:subnavigation="$section === 'all' ? null : $section"` — the "all" view explicitly asks for *no*
current child, and the first-child fallback silently overrides it. That expression is the intent;
the fallback is the bug.

Plan §2.1 line 124 claims the group's landing page "is therefore duplicated by its first child".
True for inventory / production / purchasing (parent and first child share one URL), **false for
`production-setup`**: `.../settings` vs `.../settings/numbering`. The plan is factually wrong there.

Blast radius check: of all 27 `<x-production-bench.page>` call sites, only `settings-index` relies
on the fallback, and it relies on it wrongly. Every other view either passes a matching
`subnavigation` or has `detectSubnavigationKey()` resolve it.

Fix: drop `?? $node['children'][0]`; append `$child['key']` only when a child actually resolves.
Level 2 still renders because `rows()` keys off `$path[0]`.

### My own tests pinned the bug — the lesson repeats

`ProductionBenchLayoutTest.php:93` and `:142` both assert Numbering is the current page on the
settings landing. Worse, the comment at lines 91-92 states the condition exactly — "shows every
section, so no child route matches" — and then asserts the fallback as correct. I had the diagnosis
in hand and wrote it down as the expected behaviour instead of the defect. This is the same failure
mode as the `revalidate`/`origin` review on 2026-08-29: **when a test asserts something surprising,
the surprise is the finding.** Do not rationalise it into the expectation.

### Findings I confirmed but would resolve differently

- **Dormant `[data-level='3']` CSS.** The reviewer calls it speculative and recommends deleting it.
  Plan line 227 *mandates* it ("Keep a dormant `[data-level='3']` selector"). Keep the rule; the real
  problem is the comment at `app.css:540-543`, which claims "nothing else hard-codes a depth" —
  false, `rows()` has literal `1` and `2` and `navigation.blade.php` renders `rows[1]`/`rows[2]`
  explicitly. Correct the comment instead.
- **`<p>` → `<h3>` for sub-headings.** Valid (plan line 195). Add `margin: 0` to the label rule:
  `.sk-eyebrow` sets size/weight/letter-spacing but not block margins, so UA `<h3>` margins
  (1em top and bottom) will survive and fight the token.
- **PHPDoc array shapes.** The rule is real but **not** in `.ai/rules/app.md` — it is
  `AGENTS.md:99` / `CLAUDE.md:348`: "Use array shape type definitions in PHPDoc blocks." Also note
  the rule cannot be fully satisfied: a node shape is recursive, and PHPDoc has no recursive array
  shape, so even `tree()` degrades to `children: list<array>`.

### Gap in the review

Plan line 192 specifies a `data-nav-branch` attribute on ancestors; nothing emits it — I used the
`.is-branch` class instead. The plan contradicts itself (line 218 and task 10 both say
`.is-branch`), so the implementation follows 218/288. Still worth resolving, because the other two
layout flags (`data-nav-end`, `data-nav-divider`) *are* data attributes: the three nav flags
currently use two different mechanisms.

### Fixes applied (owner approved, 2026-08-29)

- **`.sk-nav-row[data-level='1'] > .sk-nav-cluster { flex: 1 1 auto; }`** — scoped to level 1 on
  purpose. Applied to `.sk-nav-cluster` generally, level 2 would grow its first sub-group and push
  *Resources* to the far right of the rail.
- **`resolve()` fallback made conditional** on `$node['route'] === $node['children'][0]['route']`,
  and `groups()` rewritten as a `collect()->chunkWhile()->map()` pipeline.
- **`<p>` → `<h3>`**, plus `margin-block: 0` on `.sk-nav-group-label` (`.sk-eyebrow` sets
  size/weight/letter-spacing but not block margins, so UA `<h3>` margins would survive).
- **`@phpstan-type NavNode` / `NavGroup` aliases** in the class docblock, replacing bare
  `list<array>`. Recursive shape is expressible as an alias but not inline.
- Corrected the two overstated "third tier is config-only" comments (app.css and
  navigation-items.blade.php) and noted in the plan that C6 keeps the rule as instructed.
- Plan gained §8 with C4-C7.

### The fallback could not simply be deleted — verify with a probe before assuming

Removing `?? $node['children'][0]` outright broke 5 tests. A probe on `ProductionIndex` showed why:

```
http GET               page=1  level2-runs=PAGE   level1-tab=branch
livewire initial       page=1  level2-runs=none   level1-tab=PAGE
  routeIs(index) initial: false
livewire refreshed     page=1  level2-runs=none   level1-tab=PAGE
  routeIs(index) after refresh: false
  current route name: 'default-livewire.update'
```

**`routeIs()` does not work during a Livewire update request** — the route is
`livewire.update`, not the page route. Any navigation active-state logic that relies on
`request()->routeIs()` silently degrades on every live update, and the first-child default was
masking it. Views that pass `subnavigation` explicitly (inventory via `$mode`, settings via
`$section`) are immune; views that pass only `active` (the four `production-*` views and
`purchasing-index`) were relying on the default.

**Generalised: before deleting a fallback, check whether it is load-bearing in the Livewire
update path.** Testing only with HTTP `get()` will not show it.

### C7 resolved — the class was right, not the attribute

I flagged that plan line 192 named `data-nav-branch` while lines 218/288 named `.is-branch`, and
called the split "two mechanisms for three flags". On inspection the split is deliberate and
correct, so **the fix went into the plan, not the code**:

| Flag | Source | Mechanism |
| --- | --- | --- |
| `data-nav-end`, `data-nav-divider` | declarative, on the node in `tree()` | attribute |
| `.is-active`, `.is-branch` | computed per render from `$path` | class |

`.is-branch` also shares one CSS rule with `.is-active`, which an attribute selector cannot do.

**Lesson: "two mechanisms" is not automatically an inconsistency.** Check whether the two groups
differ by a real property — here, *where the value comes from* — before reporting it as a defect.
I nearly "fixed" a correct design.

### Dogfood output is not in use

The owner tried `dogfood-output/` once a few weeks ago and does not use it. Don't spend effort
regenerating `production-bench-navigation-preview.html`; it is gitignored and stale.

---

## graphify post-commit hook: root cause and fix (2026-08-29)

Symptom, on every commit made from an agent session:

```
[graphify watch] Rebuild failed: EEXIST: file already exists, mkdir '/Users/philippe/Herd/koskalk/graphify-out'
```

**It is not a graphify bug and not a project bug.** The WorkBuddy agent sandbox injects
`/Applications/WorkBuddy AI.app/.../cli/vendor/shim` via `PYTHONPATH`, whose `sitecustomize`
replaces `pathlib.Path.mkdir` with `_brokered_path_mkdir`. That replacement does not honour
`exist_ok=True`: where stdlib suppresses `FileExistsError`, it raises `PermissionError` with
**`errno is None`** and a Node-style message. `graphify/watch.py:98` calls
`out.mkdir(exist_ok=True)` on `graphify-out`, which exists on every run after the first.

Diagnosis tells: the message format (`EEXIST: file already exists, mkdir '...'`) is Node.js, not
Python (`[Errno 17] File exists:`). That mismatch is the fingerprint of a shim, not the tool.
`inspect.getsource(pathlib.Path.mkdir)` returning `_brokered_path_mkdir` confirms it.

**Fix applied** in `.git/hooks/post-commit`: run the rebuild as
`env -u PYTHONPATH CODEBUDDY_BROKERED_FS_HOOK_ENABLED=0 "$GRAPHIFY_PYTHON" -c "..."`, with a
comment block explaining why. Verified: rebuild now completes — 6877 nodes, 14642 edges,
571 communities — and `sh -n` parses.

Notes:
- `os.makedirs(..., exist_ok=True)` is **not** patched by the shim, so it also works — useful if a
  tool must run *inside* the shim.
- Commits from a normal Terminal never hit this: `PYTHONPATH` has no shim there, so the `env -u` is
  a no-op. `env -u` tolerates unset vars, verified.
- The hook is generated between `# graphify-hook-start` / `# graphify-hook-end`, so
  `graphify hook install` will overwrite the fix. Re-apply after reinstalling.
- `.git/hooks/` is not version controlled, so this lives only on this machine.

**Generalised: an error message in the wrong language's format means a shim, not the tool.** The
sandbox intercepts far more than `mkdir`.

## Owner overrides to the navigation, after seeing it rendered (2026-08-29)

The owner looked at the built UI and reversed three things. Recorded as §9 O1-O3 in
`docs/superpowers/plans/2026-08-29-production-bench-navigation-hierarchy.md`; §9 explicitly wins
over §3.4 and over correction C5 in §8.

- **O1** `.sk-nav` lost its `border-bottom`. It sat under whichever row renders last — the level-1
  tabs' 2px underline, or the level-2 rail's own border — and read as a double line either way.
- **O2** `.sk-nav-row` is now `flex-direction: column`, so each cluster owns a line. Side by side,
  the two Settings sub-groups were one run of chips separated only by a gap.
- **O3** Settings keeps its place in the reading order. `data-nav-end` now only draws a `border-l`;
  the `margin-inline-start: auto` pin and the `.sk-nav-row[data-level='1'] > .sk-nav-cluster
  { flex: 1 1 auto; }` rule that fed it are both gone — **this reverts the C5 fix from the review.**

### The non-obvious part of O2

`.sk-nav-cluster` could not simply become a column: the items are its *direct children*, so they
would have stacked too. Instead the group label is given `flex: 1 0 100%`, making it too wide to
share a flex line, so the items wrap underneath it. Markup untouched.

That also fixes a ragged-alignment trap: with the label left inline, a stacked cluster's chips
start at whatever offset its heading's translation happens to be, so "Production workflow" and
"Resources" would indent differently.

### Lessons

- **A fix that works can still be the wrong fix.** C5 was a real defect — the auto margin genuinely
  did nothing — and fixing it correctly produced a result the owner did not want. The diagnosis
  stays in the plan (it is the trap to avoid next time); the remedy was reverted.
- **Name tests after the behaviour, not the mechanism that implements it today.**
  `keeps the dashboard first and pins settings to the trailing edge` had to be renamed when the
  mechanism changed; the assertion (`data-nav-end`) was never the point.
- **When a comment says a flag "pins" or "moves" an item, that wording is itself a defect waiting to
  be filed.** The `data-nav-end` docblock is what made C5 look worth fixing. Comments now say the
  flag draws a rule.
- Suite: 2616 passed / 8 failed — same 8 as the documented baseline, one more passing than before
  (I replaced one test with two). One intermediate run showed 9 failures; a clean re-run showed 8,
  so **one of the ingredient/media failures is flaky** — do not chase a single extra failure there
  without re-running first.

## O4 — Settings now lands on Numbering, not on a page of every section

Owner feedback: clicking Settings dumped all seven sections on one page. It now lands on
Numbering, the group's first child. Recorded as §9 O4 in the navigation plan.

- `routes/web.php`: `/production/settings` became `Route::redirect(...)` to
  `.../settings/numbering`, keeping the route name. Matches the existing `/purchasing` redirect.
- `ProductionBenchNavigation::tree()`: `production-setup` route = `...settings.numbering`, the
  same as its first child.
- `resolve()`: **no code change.** The route-equality check from C4 covers it automatically once
  the routes match. Only the comment naming Settings as the exception was removed.
- `SettingsIndex::mount()`: `presets` branch deleted — that route renders `BatchSizeIndex`, so it
  could never match. View condition narrowed from `in_array($section, ['all','presets'])` to
  `$section === 'all'`.

### The interesting part

**O4 inverts C4 and makes it unnecessary.** C4 added a guard because Settings was the one group
whose landing page was not one of its children. Rather than special-case it, O4 removed the
asymmetry — the tab now points at its first child like the other three groups. The guard stays
because it is the general rule; the exception it was written for is gone.

Worth remembering as a pattern: **before adding a special case, ask whether the asymmetry that
needs it should exist.** Here the "defect" and its remedy both became moot once the underlying
inconsistency was removed.

### Left alone deliberately

The `all` rendering in `SettingsIndex` is now unreachable from the UI but is **load-bearing for
tests**: `ProductionBenchProductionSettingsTest` mounts the component with
`Livewire::test(SettingsIndex::class)`, which has no route, so `mount()` falls to
`default => 'all'` and the test exercises all five forms on that one render. Deleting the `all`
branch means rewriting that ~60-line test into five route-level tests. Flagged as the main piece
of test-only surface in the component.

### Verification

- Pint clean. Suite: 2615 passed / 8 failed — baseline set, one fewer passing than before
  (removed the obsolete `settings landing` case and the `all sections` test, added the redirect
  test).
- `php artisan route:list` confirms the redirect is registered under the same name.
- No view links to `production-bench.production.settings` any more, so nothing takes an extra hop.

## Pre-merge review of codex/production-bench-navigation-hierarchy (2026-08-29)

Reviewed the full 9-commit diff before merge. Verdict: ready, no blocking findings.

Verified during review:
- The 7 deleted JSON translation rows were stale duplicates keyed `ingredients.validation.translation_write_intent_*`; the live keys are `editor.validation.*` and are untouched. `navigation.calendar` is still used by task-index.
- `SettingsIndex` is NOT dead: departments/employees/task-types/calendar routes still render it. Only the `default => 'all'` branch is HTTP-unreachable now (kept deliberately; the settings tests mount the component directly).
- Compiled CSS bundle contains the O1-O3 rules; Pint clean; 138 targeted tests pass.

Non-blocking nits: physical border-left/right/margin-left in the nav CSS amid logical properties (RTL-only, all locales are LTR); the redirect target is a hard-coded path, same pattern as the existing `/purchasing` redirect.

## Nit resolution decision (2026-08-29)

Of the three pre-merge nits, only the logical-properties one earned its diff (committed 0dfc1fd1): the nav CSS now uses border-inline-*, margin-inline-*, padding-inline-* and logical border-radius throughout, matching the file's own convention. The hard-coded redirect target and the HTTP-unreachable `all` settings rendering were deliberately skipped — established pattern, and a test rewrite for dead-code removal is churn best done next time the settings tests are touched.
