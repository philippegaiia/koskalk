# 2026-09-05

## Reconciled the ingredient-editor audit doc with itself (continuation of 2026-09-04)

Owner asked "have you updated the audit doc?" — verifying rather than answering from memory turned
up a real defect: §4 "Proposals, in order" had **not** been updated when I amended the findings, so
it contradicted the later sections in four places:

- §4 #5 still said **build a reference page** for platform ingredients, after §6.1 had concluded
  delete-first.
- §4 #6 still said **"surface the soap-trust state"** — the exact opposite of the amended F8, which
  says *do not unhide the flag* (`.ai/rules/dashboard.md` mandates it hidden).
- §4 #2 still said **"add a dirty-state guard"**, not the reuse-the-workbench-indicator version.
- §4 #7 still listed **F7 "label the row actions"**, which §6.6 had dropped.

Fixed by rewriting §4 to 11 ranked items plus an explicit **Dropped** list, and adding a header line
stating that §4 supersedes §2 where they disagree. Marked F7's own proposal superseded inline and
added a refinement note to F6's.

**Two of my own self-corrections were overstated, and I corrected those too.** (a) §6.2 claimed I
had called the icon-only row actions "too small" — the original F7 already noted 36px clears the
24px floor, so that was never the claim; the real grounds for demoting it are that neither
escalation trigger applies (both links carry `aria-label` and `title`). (b) §6.1 claimed the ladder
"inverted F2 from build to delete" — F2's own proposal already said *stop rendering the form*; it
was only §4's summary that proposed building. The genuine refinement is narrower: don't render a new
infolist either, because the summary card at `blade.php:41` already carries INCI / CAS / EC /
allergens, and `views.md` caps how much may be shown at all.

Committed as `baaf15f8` (rules amendment) and the reconciliation on top. Doc now 418 lines,
internally consistent, nothing built — still analysis only.
