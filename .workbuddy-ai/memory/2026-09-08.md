# 2026-09-08

Continues the 2026-09-07 inventory review (badge removal). Owner replied "ok" to the offer of a
`/better` pass plus batching the two unambiguous fixes.

## Ran `better-interface`

`better-interface` and the `better-*` domain skills are **not registered in the Skill tool** — see
MEMORY.md. Loaded them by reading `~/.agents/skills/better-*/SKILL.md` directly. Recon found
`DESIGN.md` (real design system: tokens, "Green Earns Attention", "Semantic Color", "Numeric Trust",
"Structural Lines") plus `AGENTS.md` / `CLAUDE.md`.

## Shipped (owner pre-approved)

1. **Below buffer tile now filters** — `toggleBelowBufferFilter()` added to
   `app/Livewire/ProductionBench/InventoryIndex.php`; `toggleShortageFilter()` refactored to delegate
   to a private `toggleStockStateFilter(string $state)`. The tile was the only one of four that
   looked like its clickable sibling but did nothing.
2. **Sticky identity column** — `sticky left-0 z-10` on the Material `<th>` and `<td>`. The row tint
   is `/40`, so the sticky cell needs an **opaque** fill; used
   `bg-[color-mix(in_oklab,var(--color-danger-soft)_40%,var(--color-panel))]` (and the warning
   equivalent). `color-mix` already had precedent at `soapkraft.css:370`.

**43 tests pass, Pint clean.** Verified against the live Vite dev server (`public/hot` →
`https://koskalk.test:5173`) that both `color-mix(...)` and `.size-2` actually compile — do not
assume, curl the dev server and grep.

## The one HIGH: `--color-ink-muted` fails contrast

Measured (oklch → linear sRGB → WCAG, script in /tmp/contrast.py — recompute, don't trust the numbers
blind):

| pair | ratio | needs |
|---|---|---|
| `ink-muted` on `panel` | **4.11:1** | 4.5:1 (12px) — FAIL |
| `ink-soft` on `field-muted` | 6.30:1 | PASS |
| `ink-soft` on `panel-muted` | 6.29:1 | PASS |
| `warning-strong` on `panel` | 7.18:1 | 3:1 (non-text) — PASS |
| `danger-strong` on `panel` | 7.84:1 | PASS |

**I got this wrong on the first run** — gamma-encoded before computing luminance and "found" 2.51:1
everywhere. Relative luminance needs **linear** RGB. Sanity-check by converting the token to hex
first (`ink-soft` → `#5e5851`, `panel` → `#fcfaf6`); if a mid-grey reads as ~5:1 with white, the
pipeline is right.

`ink-muted` is used in ~15 more places across production-bench, several on `surface-muted` which I
did **not** measure. Reported as one systemic token finding. Left the colour alone per
`better-colors` ("leave the colors alone unless asked"); proposed the local fix = swap that one
caption to `ink-soft`.

## Also reported, not fixed

- Shortage tile `aria-label="Negative forecast"` overrides the visible count → WCAG 2.5.3 Label in
  Name. Same flaw now on the new below-buffer tile (kept consistent deliberately). One-line fix:
  append the count to the label.
- Duplicate `h1`/`h2` "Stock by material" (inventory-index:13 and :20).
- `aria-label` on a bare `<div>` for the filter chip group (:93) — ignored without a role.
- Dot's `title` is mouse-only; keyboard users cannot resolve it.

## Sticky thead (follow-up, same turn)

Owner asked for a sticky `<thead>` too. **The non-obvious part is not `sticky top-0` — it is the
scroll container.** `overflow-x: auto` computes `overflow-y` to `auto` as well, but with no height
the wrapper never scrolls, so `sticky top-0` on the thead does nothing. Fix is to bound the
wrapper: `max-h-[70dvh] overflow-auto`. Confirmed by the project's own precedent at
`production/batch-size-form.blade.php:56` (`max-h-[28rem] overflow-y-auto` + `sticky top-0 z-10`).

Used `dvh` not `vh` — the codebase already uses `max-h-[88dvh]` / `max-h-[56dvh]`
(`media-asset-picker.blade.php`). `dvh` handles mobile browser chrome.

**z-index layering matters** and is easy to get wrong: thead `z-20`, corner `<th>` `z-30`, sticky
body `<td>` `z-10`. thead must outrank the sticky body cell or the column covers the header while
scrolling down; the corner th must outrank its sibling ths while scrolling right.

Added a guard test (`keeps the material header visible while the rows scroll`) because deleting the
max-height unsticks the header while all data assertions still pass. 44 tests pass, Pint clean.

## Not reviewed

Colors beyond the five measured pairs; dark mode; rendered 320px / 200% zoom.

## Lot register analysis (tab 2) — analysis only, no code changed

Owner asked for the same pass on the second tab. Committed tab 1 first (3 commits, unpushed, left
HEAD on `main`) so the two bodies of work stay separable.

`.ai/rules` check: `resources/views/**` → `views.md` and `app/Livewire/ProductionBench/**` →
`livewire-production-bench.md`; neither constrains this screen.

**Verdict: Approve — no HIGH, no contrast blocker.** 10 pairs measured, all pass: thead 6.29, name
17.17, mono codes/dates 6.69, header caption 5.28, accent links 8.10 (and 8.93 if the app.css value
wins over soapkraft's), Released pill 10.33, Quarantined pill 6.32.

Findings reported to the owner (ranked):

1. **MEDIUM — "Clear filters" clears only the material.** `clearLotMaterial()` (`InventoryIndex.php:
   598`) resets 3 properties; the button is labelled `inventory.clear_filters`. It renders only when
   `$lotMaterialLabel` is set, so with a supplier + date + status + expiry filter there is **no reset
   affordance at all**. Tab 1 does this right: `clearMaterialFilters()` (`:606`) resets 8, plus a
   removable chip per active filter (`:117-120`).
2. **MEDIUM — no sticky thead and no sticky identity column**, on the denser of the two tables
   (9 columns, `min-w-[1120px]`, up to 5 stacked lines/row). Tab 1 has both (`max-h-[70dvh]
   overflow-auto`, thead `sticky top-0 z-20`, `th/td sticky left-0`).
3. **MEDIUM — empty state can lie.** `:294` shows `no_open_lots` = "No open lots for this material."
   whenever `$lotScope === 'open'`, even with no material selected. That key is shared with
   `inventory-material-detail.blade.php:95` where the wording is correct, and
   `ProductionBenchInventoryMaterialDetailTest.php:278` pins it there — so the fix is a new key or a
   conditional, **not** rewording the shared string.
4. **MEDIUM — help text is orphaned.** `id="lot-register-search-help"` (`:225`) is referenced by
   nothing. Same for tab 1's `inventory-search-help` (`:113`). The house pattern does wire it —
   `GoodsReceiptDocumentUiAndInventoryTest` asserts `aria-describedby="receipt-document-upload-help
   receipt-document-upload-error"`.
5. LOW — 11 always-visible filter controls, every one `->live()`; tab 1 puts advanced filters behind
   a disclosure.
6. LOW — `inventory.quarantined` = 'Quarantined' is both the status value and the quantity column
   header (`:239` vs `:242`).
7. LOW — row action `min-h-9 px-2` is 36px tall; passes WCAG 2.5.8 (24px), narrowest control on the
   row. Note only, not a defect.

**Retracted a hypothesis I had formed:** I assumed the missing `relative z-30` on the lot filter bar
(`:223`) was a defect carried over from tab 1. It is not — the tab-2 thead is not sticky, so the
Filament dropdown has nothing to collide with. Adding it would be cargo-culting the tab-1 fix.

Checked and clean (do not re-file): status pill is not colour-only (carries a word); the
`released ? success : warning` ternary is exhaustive — `StockLotStatus` has exactly 2 cases;
`$lot->stocked_at->format()` is safe, the column is `date` NOT NULL; `colspan="9"` matches 9 `<th>`.

## Implemented (owner said "evaluate again and proceed")

Sticky thead + sticky identity column on the lot register, then the three remaining MEDIUMs.

- **Sticky**: wrapper `max-h-[70dvh] overflow-auto`, thead `sticky top-0 z-20` + the
  `shadow-[0_1px_0_0_var(--color-line)]` rule, corner `<th>` `sticky left-0 z-30`, identity `<td>`
  `sticky left-0 z-10` with **opaque** `bg-[var(--color-panel)]`. The identity cell needs no
  `color-mix` here (unlike tab 1) because **lot rows carry no tint** — check for a row tint before
  reaching for `color-mix`.
- **The retraction came back**: adding the sticky thead *created* the collision I had retracted, so
  the lot filter bar needed `relative z-30` after all. "Not a defect as it stands" and "not needed
  once you change X" are different claims — say which one you mean.
- **`clearLotFilters()`** resets all 12 lot properties to their `#[Url(except:)]` defaults (so the
  query string empties) + `lotFilters.lotMaterialSelection = null`. New `lotFiltersActive()`
  mirrors `materialFiltersActive()` but **excludes `lotSort` and `lotDateBasis`** — a sort changes no
  rows, and the date basis is inert without a date; showing "Clear filters" for either would promise
  a reset for nothing. Pinned by a test.
- **Empty state**: `no_open_lots` ("...for this material") was shown for any empty open scope. Now
  `$lotFiltersActive ? no_lots_match : no_lots`. Did **not** reword the shared key — it is correct on
  `inventory-material-detail.blade.php:95` and pinned by `ProductionBenchInventoryMaterialDetailTest.
  php:278`.
- **aria-describedby**: Filament does **not** associate helper copy with its inputs (verified: only
  `Section`/`FusedGroup`/`modal` in vendor set it). `->extraAttributes(['aria-describedby' => ...])`
  does land on the `<input>` — confirmed by assertion, not by reading vendor.

## Filters disclosure + the two-search-field question

Owner noticed the lot register had no Filters toggle and that "Search" and "Material" read as two
search boxes. Split the schema into `lotFiltersForm` (search + `lotSort`) and a new
`lotAdvancedFiltersForm` (the 8 narrowing filters + the material combobox), behind the same
`aria-controls`/`aria-expanded`/`x-data="{ filtersOpen: @js($lotFiltersActive) }"` disclosure as tab
1. Moving the material combobox into the collapsed panel leaves **one** visible search box, which
answers both observations at once.

**They do genuinely overlap** — `search` is a `LIKE %term%` across lot code, supplier batch,
ingredient display/INCI/soap names + translations, workspace `material_code`, packaging name/code,
supplier SKU/item name and supplier name; `lotMaterialSelection` is an exact `type:public_id`
resolution. Same rows for a simple "olive" query, but search is fuzzy and far broader while the
combobox is exact and drives the chip + deep link. Not merged — that is a product call.

Moving controls to a second schema **changes their rendered ids** (`lotFiltersForm.lotScope` →
`lotAdvancedFiltersForm.lotScope`), which broke 4 existing assertions. Budget for that.

## Declined after a closer look (LOW #6: "Quarantined" does double duty)

`inventory.quarantined` = 'Quarantined' is both a status value and the quantity column header, on
*both* tabs (`inventory-index.blade.php:139`, `:274`) as well as the status pills (`:317`,
`inventory-material-detail.blade.php:88`).

**Not changing it.** The catalogue's existing translations for that key are already the
"in quarantine" phrasing (de "In Quarantäne", es "En cuarentena", fr "En quarantaine",
it "In quarantena", nl "In quarantaine", pt_BR "Em quarentena"). Splitting the key would produce
near-identical strings in all six locales — churn with no observable difference — and forcing a
distinct *state* adjective ("quarantined" as an adjective) would mean inventing awkward forms in
six languages for a LOW. The shared word is domain-correct.

Also left alone: row action `min-h-9 px-2` (36px) — passes WCAG 2.5.8 (24px minimum). Not a defect.

**Scope is the exception to the disclosure.** `lotScope` stayed in the visible form alongside
`search` and `lotSort` while the other 8 narrowing filters moved in. Reason: scope defaults to
`open`, so it is the one control whose default is *not* "everything" — collapsing it would leave no
trace of why exhausted lots are missing from the register, which is the same failure mode as the
empty state claiming "no stock" when a filter hid everything. Tab 1's analogous `stockState`
defaults to `all`, so hiding it costs nothing there. When a control's default already hides rows,
keep the control visible.

## `x-cloak` lives in a Blade layout, not the stylesheet

`[x-cloak] { display: none !important; }` is at `resources/views/layouts/app-shell.blade.php:11` —
**not** in `resources/css/**`, so grepping the CSS finds nothing and looks like a bug. Production
bench pages reach it via `Route::view(...)` → `production-bench/inventory.blade.php`, which
`@extends('layouts.app-shell')`. Livewire components here are wrapped by a route view, so there is
no `#[Layout]` attribute and no `config/livewire.php` layout to find.

## Traps hit again this session

- **The contrast double-gamma trap, a third time.** `oklch_to_lrgb` already returns **linear** sRGB,
  so luminance is just `0.2126*r + 0.7152*g + 0.0722*b`. Applying the sRGB transfer function inside
  `lum()` anyway turned every real PASS into a FAIL (6.69 → 2.58, 10.33 → 4.01). Symptom: *every*
  pair fails, including near-black on near-white. Pin one anchor before trusting the table;
  `ink-soft` on `panel` = **6.690:1** (I hand-computed 6.735 with 4-digit logs — the exact value is
  6.690, and my own earlier recording of 6.69 was right).
- **Bash `grep` is unreliable in this shell generally**, not just for `\|` alternation — plain
  multi-pattern greps returned nothing twice today (once silently, once with exit 1) where the Grep
  tool found matches immediately. Default to the Grep tool.

## Lot register commits (tab 2)

Two commits on `main`, unpushed:

- `04c2f027` — sticky thead + sticky identity column in a `max-h-[70dvh] overflow-auto` wrapper;
  `relative z-30` on the filter bar; a Filters disclosure holding the seven narrowing filters with
  `lotScope` left outside it; `clearLotFilters()` resetting every field; `no_lots_match` empty state;
  `aria-describedby` on both search inputs.
- `480a5f3c` — the lot material chip became dismissible (`clearLotMaterial`), matching the materials
  tab. It had been a static `<span>` beside a "Clear filters" button that resets all ten.

Suite after: 3266 passed / 25 skipped, Pint clean.

### Decisions made, and why

- **Search vs Material: not merged.** `search` is a `LIKE %term%` sweep across lot code, supplier
  batch, ingredient display/INCI/soap names + translations, workspace `material_code`, packaging
  name/code, supplier SKU/item name and supplier name. `lotMaterialSelection` is an exact
  `type:public_id` resolution driving the chip, URL param and deep link. They overlap for simple
  cases but are not equivalent, so the fix was to move Material behind the disclosure rather than
  delete it — one visible search box.
- **`lotScope` stays outside the disclosure.** Its default `open` already hides rows; hiding it
  would recreate the unexplained-empty-state failure the empty-state fix was about.
- **No full chip row on tab 2.** The disclosure auto-opens when `lotFiltersActive`, so per-filter
  chips would be redundant — unlike tab 1, where they predate that behaviour. Not a defect.

### Operator note

Owner said **"dont ask, you decide"** — stop requesting permission for judgment calls on this
review. Commit freely, keep deciding, but still do not push without a word.

## Page scroll instead of a capped table (commit `d5676b6f`)

Owner: "if we keep a max height then there is no point in have a 'rows per page' selector". Correct,
and it applied to both tabs. Height cap removed from both; the page scrolls and the thead sticks to
the viewport. Implementation is a **container query**, not a media query: the card gets
`@container overflow-clip`, the wrapper `overflow-x-auto @min-[70rem]:overflow-x-visible`
(materials, floor 1120px) / `@min-[65rem]` (lots, floor 1040px). Below the threshold you keep
horizontal scroll and lose the sticky header — verified in headless Chrome, not assumed.

Also in that commit: the lot status pill became a dot, freeing the width that was pushing the
stocked-on date onto a second line (date is now `whitespace-nowrap`).

### Dot colour: the owner was right, `-strong` is wrong for dots

I first used `success-strong` / `warning-strong` — `#00422e` (11.2:1) and `#8a3f04` (7.2:1). At
8–10px both read as "a dark dot". Switched to the **base** tones `#257055` (5.78:1) and `#b45307`
(4.86:1): 120° apart in hue, both mid-lightness, both clear 3:1 non-text.

Rule worth keeping: `-strong` tokens exist for *text on soft backgrounds*, where you want maximum
contrast. For a small filled shape that has to be told apart from another small filled shape, use the
base tone — hue separation and mid lightness beat raw contrast. The below-buffer dot on the
materials tab still uses `warning-strong`; that dot has no sibling state to be distinguished from,
so darkness costs it nothing. Left alone deliberately.

### Operator note

Owner said **"dont ask, you decide"** — stop requesting permission for judgment calls on this
review. Commit freely, keep deciding, but still do not push without a word.

## Measured table floors (commit `7d10b497`), pagination (commit `c24be44d`)

Floors were guesses carried over from the old bounded layout. Measured them: render the component,
then set the table to `width: min-content` and read it back — **Stock by material 834px, Lot
register 952px**, before any cell wraps. Floors now 880 / 992 with the container-query threshold
equal to the floor. That drops the sticky-header threshold from a 1456px window to ~1220px
(materials) and ~1330px (register).

Pagination: the select went to h-8 globally earlier, which left page buttons at h-9 — every table
using `x-table-pagination` had a row mixing 32 and 36px. All 32px now. Also dropped a stray
`text-xs` from the register's "Clear filters" (on a 40px `.sk-btn` that is a big button, small text).

### Still open, genuinely the owner's call

- **"Shortage" vs "Negative forecast"** — one state, two names across the screen. Renaming touches
  `lang/en` plus all six catalogue locales, so it is a copy decision, not a code one.

## Row clickability (commit `a01190c5`)

Owner: "maybe we should make the row clickable instead of have the link to the detail only on the
arrow". **Premise was half wrong** — the materials tab was *already* a whole-identity-cell block
link (`-m-2 flex min-h-11 … justify-between`, arrow `aria-hidden`); only the lot register was
name+arrow-only, with material code, lot code, batch and expiry sitting below the anchor.

Fixed the lot register to mirror tab 1. **Deliberately the cell, not the whole row** — the lot row
already carries the receipt link (identity column) / supplier link (supplier column) and the
quarantine/release action in the last column, so a row-wide target would have to swallow both, and
row-wide JS handlers are not focusable. Quantity columns stay plain text so numbers remain
selectable — people copy lot codes. The receipt line stays *outside* the anchor: different
destination, and an `<a>` cannot nest in an `<a>`.

Test asserts the metadata is *inside* the anchor by slicing the rendered html from
`href="<detail url>"` to the next `</a>`, rather than by counting strings — `Testable::html()`
exists (`vendor/livewire/livewire/src/Features/SupportTesting/Testable.php:127`).

Blade pattern for "wrap this block in an `<a>` only when there is a url": conditional open/close
tags (`@if … <a …>` / `@else <div>` … `@endif` … `@if … </a>` / `@else </div>` / `@endif`). Ugly,
but it keeps one copy of the inner markup; use `<span class="block">` children so the same lines
work in both branches.

Suite: 3269 passed / 25 skipped, Pint clean. 12 commits ahead of `origin/main`, none pushed.

## Screen-level leftovers (commit `bff881cc`)

Both tabs done, so I closed out the findings I had reported for the screen as a whole.

- **Duplicate heading.** Both cards opened with a visible `<h2>` identical to the page `<h1>`
  (the screen shows one section at a time). Fixed the house way, not by inventing one:
  `production-index.blade.php:56` already has an `sr-only` `<h2>` for exactly this collision, so the
  heading stayed (it is the region's accessible name via `aria-labelledby`) and only went visually
  hidden. Note `numbering-settings.blade.php:17` has the *same* duplication unfixed — the codebase
  is not consistent about it.
- **`--color-ink-muted` at 4.11:1** — the two 12px captions on this screen
  ("12 materials · 3 without demand", "Quantities shown in …") moved to `ink-soft` (6.69:1). Local
  fix only; the token is still used ~15 more times across production-bench and still fails.
- **`aria-label` on a bare `<div>`** (the filter chip row) — added `role="group"`. A generic div has
  no role, so the name was being dropped entirely.

### Flaky test, not ours

`IngredientIntakeDuplicateTest` > "does not treat identity-bearing parenthetical … as exact" failed
once in a full run, then passed alone, passed in a two-file run with my file, and passed on a full
re-run. Cause is visible in `IngredientDuplicateDetector::exactCandidate()` (`:180`): it matches on
`display_name` as well as `inci_name`, and both the ingredient's `display_name` and the intake item's
`original_current_name` are **faker-random** — a rare collision makes `exact` non-empty. Pre-existing
flakiness; do not chase it as a regression.

Suite after: 3270 passed / 25 skipped, Pint clean. 14 commits ahead of `origin/main`, none pushed.

## One name for the shortage state (commit `cbe1419f`)

The tile counted the state as **"Shortage"** (`production_bench.production.shortage`) while the stock
state filter and its chip called the same thing **"Negative forecast"**
(`inventory.filter_negative_forecast`).

Unified on the tile's wording by **deleting the second key** rather than retuning it — the filter
option (`InventoryIndex.php:262`) and the chip (the `filter_.$stockState` dynamic key in the blade)
now both use `production_bench.production.shortage`, which was already translated in all six
catalogue locales (de "Fehlmenge", es "Faltante", fr "Manquant", it "Mancante", nl "Tekort",
pt_BR "Falta"). So the rename cost **no new translations** — reuse an existing localized key instead
of inventing six. `negative_forecast` stays the internal state value; only the label changed.

Two places build that label, and only one is obvious: `InventoryIndex.php:262` (the Filament Select
options) and the blade's `__('production_bench.inventory.filter_'.$stockState)`, which derives the
key at runtime and so does **not** show up when you grep for the string. Grep the *state value*, not
the translation key.

Tests that pinned the old wording: `ProductionBenchPagesTest` `assertSee('Negative forecast')` →
`__('production_bench.production.shortage')`; the French test `Prévision négative` → `Manquant` and
`assertDontSeeHtml('>Negative forecast<')` → `>Shortage<` (keeps the same strength: French present,
English absent).

Suite after: 3270 passed / 25 skipped, Pint clean. 15 commits ahead of `origin/main`, none pushed.

## Responsive check of the two tables (measurement only, nothing changed)

Verified the container-query layout I shipped, because I had only ever measured it at 500px and
1400px. Harness: dump `Livewire::test(...)->html()` to a file from a scratch test, inline the **live**
CSS (fetch `https://koskalk.test:5173/resources/css/app.css`, then unescape the `__vite__css = "…"`
JS string — a regex capture is not enough, scan for the closing quote while skipping `\\` escapes),
rebuild the shell (`max-w-[2100px]` + `lg:grid-cols-[17rem_minmax(0,1fr)]` sidebar + `<main
class="flex-1 px-6 py-8 lg:px-8">`), then run headless Chrome and read results back out of a
`data-result` attribute with `--dump-dom`.

**Chrome clamps `--window-size` to a 500px minimum** — asking for 320/375/414 all report
`innerWidth=500`. I wasted a cycle thinking I had phone measurements. Below 500 you must either force
a width on a wrapper element or use real device emulation.

Measured (card = the section's own width, sidebar 272px + `lg:px-8` 64px + 15px scrollbar):

| window | card | materials | lot register |
|---|---|---|---|
| 768 | 705 | no stick | no stick |
| 1024 | 673 | no stick | no stick |
| 1280 | 929 | **sticks** | no stick |
| 1440 | 1089 | **sticks** | **sticks** |

So the sticky thresholds are ~**1231px** window for materials (card ≥ 880) and ~**1343px** for the
register (card ≥ 992) — which matches the ~1220/~1330 I had predicted. Below them, `overflow-x-auto`
wins and the thead scrolls away; that is the designed fallback, and it cannot be avoided, because
`overflow-x: auto` computes `overflow-y` to `auto` too and `overflow-y: visible` is not honoured
alongside it.

**No page-level horizontal overflow at any width**, down to a forced 320px shell (measured by putting
`width:320px` on the shell div with a 900px window, i.e. no clamping): `scrollWidth - clientWidth = 0`
for both tabs, and nothing outside the scroll wrapper crosses the viewport edge.

Two traps in the same exercise: (a) `getBoundingClientRect()` ignores ancestor clipping, so the table
inside `overflow-x-auto` always *looks* like it overflows — exclude `el.closest('div.overflow-x-auto')`
before hunting offenders; (b) root-level CSS `zoom` to fake a narrower viewport produced a bogus 95px
overflow at zoom 2 and 0 at zoom 1.6 — `documentElement.clientWidth` stayed at the unzoomed 485 while
descendants doubled, so the two are not comparable. Don't use zoom for this.

**Left for the owner, not done:** at a 1280px window — the common laptop — the register header does
not stick while the materials one does. Closing that means dropping the register's 992px floor by
~65px: either `px-4` → `px-3` across its 8 columns (saves 64px, lands at ~888 vs 929 available — a
1px margin, too fragile) or hiding a column. `reserved` is derivable (`available = physical -
reserved - quarantined`), so it is the candidate to drop, but that is a product decision about which
numbers stay visible. Not done unilaterally.

## Material detail screen — sticky headers (commit `dcfbc4ee`)

`inventory-material-detail.blade.php` is where the enlarged identity links on both tabs land, so it
got the same treatment. Three tables, none with a sticky thead: open lots (`min-w-[900px]`), supplier
listings (`min-w-[860px]`), period activity (`min-w-[760px]`). Same recipe as the index — card
`overflow-hidden` → `overflow-clip` plus `@container`, wrapper `overflow-x-auto
@min-[Nrem]:overflow-x-visible` with N just over each floor: **57 / 54 / 48rem**.

The activity card also needed the `relative z-30` on its filter wrapper that the index filter panels
carry — its Filament select would otherwise open behind the new thead. **Any card that gets a sticky
thead and also holds a Filament form needs that stacking context.**

Deliberately **not** done: sticky first columns — all three floors fit the ~929px card at desktop
widths, so there is no horizontal scroll to survive; the painful axis here is vertical (activity at
25/page). And the open-lots status **pill** stays: the register's dot works only because the last
column's Quarantine/Release button already names the state, and this table has no such column.
Different condition, different call — consistency for its own sake would delete the only cue.

## Page size control announced the wrong name (commit `7d243f56`)

`x-table-pagination` rendered a hard-coded `table.pagination.rows_per_page` as the visible text but
used the `perPageLabel` prop only as the select's `aria-label`. **`aria-label` beats the wrapping
`<label>`**, so the accessible name never contained the visible text — WCAG 2.5.3, Level A. Four
production-bench sites made it worse by passing a *section title* ("Stock by material", "Lot
register") where a per-page label belongs, so a screen reader announced the table name for a
page-size select.

Fix: the prop **is** the visible label now (`perPageLabel ??= __('table.pagination.rows_per_page')`
as the fallback), and the select points at the span with `aria-labelledby` instead of overriding it.
Ids are `per-page-{model}-{pageName}` because the detail screen renders two paginators — note the
materials paginator's page name is **`materials`**, not `page`; a test guessing
`per-page-perPage-page` fails.

Three new keys (`inventory.materials_per_page|lots_per_page|movements_per_page`) × 6 locales.
**Catalogue edits are safe to rewrite with Python**: `json.dumps(indent=2, ensure_ascii=False) + '\n'`
is *byte-identical* to the file, so load → insert at the `bisect` position of `group.key` → dump
gives a minimal diff. Insert by index; do not re-sort globally. Then
`artisan translations:catalogue:import --mode=preserve-existing` (additive — reported "150 created,
0 updated, 204 production values preserved"), and **verify with tinker in de/fr/nl**, since a missing
row silently falls back to English and looks like success.

Visible copy changes app-wide as a side effect: "Rows per page" → "Ingredients per page", "Packaging
items per page", etc. Broke two tests asserting the old string plus one French localization test that
seeded `table.pagination.rows_per_page` instead of `ingredients.table.per_page`.

Suite after: **3273 passed / 25 skipped**. Pint: 5 failures, all pre-existing inventory
service/query files, none in files I touched. 20 commits ahead of `origin/main`, none pushed.
