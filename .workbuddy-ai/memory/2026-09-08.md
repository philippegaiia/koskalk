# 2026-09-08

Continues the 2026-09-07 inventory review (badge removal). Owner replied "ok" to the offer of a
`/better` pass plus batching the two unambiguous fixes.

## Ran `better-interface`

`better-interface` and the `better-*` domain skills are **not registered in the Skill tool** — see
MEMORY.md. Loaded them by reading `~/.agents/skills/better-*/SKILL.md` directly. Recon found
`DESIGN.md` (real design system: tokens, "Green Earns Attention", "Semantic Color", "Numeric Trust",
"Structural Lines") plus `AGENTS.md` / `CLAUDE.md`.

## Shipped (owner pre-approved)

1. **Below buffer tile now filters** — `toggleBelowBufferFilter()` added to
   `app/Livewire/ProductionBench/InventoryIndex.php`; `toggleShortageFilter()` refactored to delegate
   to a private `toggleStockStateFilter(string $state)`. The tile was the only one of four that
   looked like its clickable sibling but did nothing.
2. **Sticky identity column** — `sticky left-0 z-10` on the Material `<th>` and `<td>`. The row tint
   is `/40`, so the sticky cell needs an **opaque** fill; used
   `bg-[color-mix(in_oklab,var(--color-danger-soft)_40%,var(--color-panel))]` (and the warning
   equivalent). `color-mix` already had precedent at `soapkraft.css:370`.

**43 tests pass, Pint clean.** Verified against the live Vite dev server (`public/hot` →
`https://koskalk.test:5173`) that both `color-mix(...)` and `.size-2` actually compile — do not
assume, curl the dev server and grep.

## The one HIGH: `--color-ink-muted` fails contrast

Measured (oklch → linear sRGB → WCAG, script in /tmp/contrast.py — recompute, don't trust the numbers
blind):

| pair | ratio | needs |
|---|---|---|
| `ink-muted` on `panel` | **4.11:1** | 4.5:1 (12px) — FAIL |
| `ink-soft` on `field-muted` | 6.30:1 | PASS |
| `ink-soft` on `panel-muted` | 6.29:1 | PASS |
| `warning-strong` on `panel` | 7.18:1 | 3:1 (non-text) — PASS |
| `danger-strong` on `panel` | 7.84:1 | PASS |

**I got this wrong on the first run** — gamma-encoded before computing luminance and "found" 2.51:1
everywhere. Relative luminance needs **linear** RGB. Sanity-check by converting the token to hex
first (`ink-soft` → `#5e5851`, `panel` → `#fcfaf6`); if a mid-grey reads as ~5:1 with white, the
pipeline is right.

`ink-muted` is used in ~15 more places across production-bench, several on `surface-muted` which I
did **not** measure. Reported as one systemic token finding. Left the colour alone per
`better-colors` ("leave the colors alone unless asked"); proposed the local fix = swap that one
caption to `ink-soft`.

## Also reported, not fixed

- Shortage tile `aria-label="Negative forecast"` overrides the visible count → WCAG 2.5.3 Label in
  Name. Same flaw now on the new below-buffer tile (kept consistent deliberately). One-line fix:
  append the count to the label.
- Duplicate `h1`/`h2` "Stock by material" (inventory-index:13 and :20).
- `aria-label` on a bare `<div>` for the filter chip group (:93) — ignored without a role.
- Dot's `title` is mouse-only; keyboard users cannot resolve it.

## Sticky thead (follow-up, same turn)

Owner asked for a sticky `<thead>` too. **The non-obvious part is not `sticky top-0` — it is the
scroll container.** `overflow-x: auto` computes `overflow-y` to `auto` as well, but with no height
the wrapper never scrolls, so `sticky top-0` on the thead does nothing. Fix is to bound the
wrapper: `max-h-[70dvh] overflow-auto`. Confirmed by the project's own precedent at
`production/batch-size-form.blade.php:56` (`max-h-[28rem] overflow-y-auto` + `sticky top-0 z-10`).

Used `dvh` not `vh` — the codebase already uses `max-h-[88dvh]` / `max-h-[56dvh]`
(`media-asset-picker.blade.php`). `dvh` handles mobile browser chrome.

**z-index layering matters** and is easy to get wrong: thead `z-20`, corner `<th>` `z-30`, sticky
body `<td>` `z-10`. thead must outrank the sticky body cell or the column covers the header while
scrolling down; the corner th must outrank its sibling ths while scrolling right.

Added a guard test (`keeps the material header visible while the rows scroll`) because deleting the
max-height unsticks the header while all data assertions still pass. 44 tests pass, Pint clean.

## Not reviewed

Colors beyond the five measured pairs; dark mode; rendered 320px / 200% zoom; the lot-register
branch of the same screen.
